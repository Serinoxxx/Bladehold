# AGENTS.md

**Read `/CLAUDE.md` at the repo root. It is the single source of truth for this project** (overview, current game loop, design direction, conventions, legacy code, git workflow). Nested `CLAUDE.md` files next to the code in `Assets/Bladehold/Bladehold Scripts/` (Campaign, Waves, Fort, DamageSystem, Enemies) hold the per-system detail.

Don't add project knowledge here; update `CLAUDE.md` instead.

Project skills live in `.agents/skills/` (Antigravity) and `.claude/skills/` (Claude Code).
