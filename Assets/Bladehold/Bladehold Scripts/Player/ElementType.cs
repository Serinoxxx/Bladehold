/// <summary>
///     The elements the Mage's imbuement can hold (see <see cref="MageImbuement" />). Serialized into
///     <see cref="ElementNode" /> / <see cref="Runestone" /> prefabs and per-element visual lists —
///     append new elements (wind, death, …) at the end, never reorder.
/// </summary>
public enum ElementType
{
    Fire,
    Lightning,
    Ice,
}
